name: Keep Streamlit Awake

on:
  schedule:
    - cron: '*/5 * * * *'

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping the Streamlit app
        run: curl -m 10 -s https://opp-business-log-gjxekgwnpjmruvuvkrapfv.streamlit.app/ || true
